{
  "name": "marketplace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && NODE_OPTIONS='--max_old_space_size=3072' npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev",
    "vercel-build": "cd frontend && NODE_OPTIONS='--max_old_space_size=3072' npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
} 